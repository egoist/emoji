{
  "private": true,
  "name": "emoji-search",
  "productionName": "Emoji Searcher: find the emoji that echoes your mind",
  "description": "A modern emoji searcher.",
  "version": "0.0.0",
  "main": "src/index.js",
  "repository": {},
  "scripts": {
    "test": "echo lol",
    "lint": "vbuild -c --eslint",
    "dev": "vbuild -dc",
    "build": "vbuild -c",
    "surge": "npm run build && surge -p dist -d emoji-search.surge.sh",
    "deploy": "npm run lint && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "vbuild": "^6.19.0"
  },
  "license": "MIT",
  "dependencies": {
    "babel-polyfill": "^6.23.0",
    "clipboard": "^1.6.0",
    "emojilib": "^2.1.0",
    "fuse.js": "^2.6.2",
    "lodash.debounce": "^4.0.8",
    "native-toast": "^1.1.0",
    "offline-plugin": "^4.6.1"
  }
}
