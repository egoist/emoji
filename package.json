{
  "private": true,
  "name": "emoji-search",
  "description": "A modern emoji searcher.",
  "version": "0.0.0",
  "main": "src/index.js",
  "repository": {},
  "scripts": {
    "test": "echo lol",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "surge": "npm run generate && surge -p dist -d emoji-search.surge.sh",
    "deploy": "npm run generate && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0"
  },
  "license": "MIT",
  "dependencies": {
    "clipboard": "^1.6.0",
    "emojilib": "^2.1.0",
    "fuse.js": "^2.6.2",
    "lodash.debounce": "^4.0.8",
    "native-toast": "^1.1.0",
    "nuxt": "^0.9.9"
  }
}
